
  
  library(ggplot2)
  search()
  library(slidify)

  library(devtools)
  find_rtools()
  
  library(KernSmooth)
  
  
  # git index: git add -A
  # commitment: git commit -m "message"
  # pushing: git push